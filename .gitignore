.build/
build/
.swiftpm/

# Dev-channel app icon — generated locally on the first `./dev.sh`, not shipped.
apps/desktop/Resources/AppIcon-Dev.icns
.DS_Store
node_modules/
.turbo/
dist/
.vite/

# Convex / env — local deployment URLs and secrets, never committed
.env.local
.env.*.local

# Convex CLI "AI files" scaffolding (opt-in via `convex ai-files install`)
AGENTS.md
.agents/
skills-lock.json
convex/_generated/ai/
